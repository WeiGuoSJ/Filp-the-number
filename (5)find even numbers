#将20位学生按1-20的序号顺序排列，使用 while 循环判断计数器counter大小,于21则执行循环体，循环体内通过if判断实现，将偶数结果通过append()函数连接，最后以列表的形式输出所有的偶数。
#提示：请按照从小到大的顺序输出，不包括负数哦～


counter = 1
evenNumber = []
while counter < 21:
    if counter % 2 == 0:
        evenNumber.append(counter)
    counter = counter + 1
print(evenNumber)
